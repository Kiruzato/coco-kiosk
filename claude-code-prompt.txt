Act as a senior systems architect reviewing the current Campus Query Engine (CQE) implementation.
There is a new finalized schema defined in schema.txt. Your task is to evaluate whether the current CQE implementation fully supports this schema.
Objectives
1.	Review the new schema in schema.txt.
2.	Analyze the current CQE components:
o	Parser
o	StructuredQuery model
o	Filter extraction logic
o	Index builder
o	Executor
o	Response formatter
3.	Determine whether the CQE can correctly:
o	Load and index the new schema
o	Handle tag-based entities
o	Resolve Room and OutdoorLocation entities
o	Support filtering, counting, listing, and nearest queries using the updated model
o	Maintain deterministic guarantees
Required Output
•	Clearly state whether the current CQE is fully compatible with the new schema.
•	If it is not fully compatible, identify:
o	What parts break or are incomplete
o	What structural changes are required
o	What indexing changes are required
o	What parser or filter changes are required
•	Propose the necessary architectural and implementation adjustments to ensure full compatibility.
•	Maintain deterministic-first behavior and avoid introducing LLM-based inference into structured execution.
Do not assume compatibility — verify it explicitly.
Do not implement admin features.
Note: After this compatibility review and any required adjustments, the next development phase will focus on Admin Directory Management. Do not implement admin functionality at this stage.
Focus strictly on ensuring CQE is structurally aligned with the new schema and remains architecturally clean, deterministic, and extensible.

